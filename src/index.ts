import { buildOasSpec, oasWebpackLoader } from './oasWebpackLoader';
import { OasLoaderOptions } from './types';

export default oasWebpackLoader;

export { buildOasSpec };
export { OasLoaderOptions };
